# Manual-only measure lines

Accurate automatic barlines would require every bar’s note lengths to sum exactly to the intended beat count. Enforcing that on creators would add friction without enough payoff, so Measure lines stay manually placed (with optional sticky BPM) and are never auto-inserted from note lengths.

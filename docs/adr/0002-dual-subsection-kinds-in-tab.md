# Dual subsection kinds (tab + chord) in one Tab

Creators need both fretboard notation and chord/strumming charts in the same song. Each titled Section can contain tab subsections and chord subsections rather than separate tab vs chord documents. That unifies progression, playback compilation, and editing, but forces every consumer (UI, compiler, screenshots, audio) to handle two shapes.

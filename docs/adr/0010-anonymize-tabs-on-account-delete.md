# Anonymize tabs on account deletion

When a User deletes their account they may keep their Tabs in the library with authorship cleared (Anonymous) instead of cascading deletes. That preserves community content and bookmarks while still removing the identity; write paths and UI must treat null creators as first-class.
